import { ReactElement } from "react";
import styled from "styled-components";

function EditorTitle(props): ReactElement {
  return <StyledWrap></StyledWrap>;
}

const StyledWrap = styled.div(() => {
  return {};
});

export default EditorTitle;
